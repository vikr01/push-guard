{
  "name": "push-guard",
  "version": "0.1.0",
  "description": "Git push authorization manager for Claude Code hooks",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vikr01/push-guard"
  },
  "bin": {
    "push-guard": "./push-guard"
  },
  "scripts": {
    "build": "swc install.ts -o install.js",
    "prepublishOnly": "npm run build",
    "postinstall": "node install.js"
  },
  "files": [
    "install.js"
  ],
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "@swc/cli": "^0.8.0",
    "@swc/core": "^1.15.11",
    "@types/node": "^25.3.0"
  }
}
