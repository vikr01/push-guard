{
  "name": "push-guard",
  "version": "0.1.0",
  "description": "Git push authorization manager for Claude Code hooks",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vikr01/push-guard"
  },
  "bin": {
    "push-guard": "./push-guard"
  },
  "scripts": {
    "build": "bun build install.ts --target=node --format=cjs --outfile=install.js",
    "prepublishOnly": "bun run build",
    "postinstall": "node install.js"
  },
  "files": [
    "install.js"
  ],
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "@types/node": "^25.3.0"
  }
}
